spring:
  application:
    name: simulador-core-legado
  mvc:
    problemdetails:
      enabled: true

server:
  port: 8090

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
